import React from "react";
import { render } from "@testing-library/react";
import Recipe from "../SockComponents/Recipe";

test("Toe up recipe should render with short row heel", () => {
  /* const { container } = render(
    <Recipe direction="toeup" heeltype="shortrow" />
  );
  expect(container.firstChild).toMatchSnapshot(); */
});
